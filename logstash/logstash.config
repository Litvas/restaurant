input {
  tcp {
      port => 5044
      codec => json
      }
}

output {
  elasticsearch {
    hosts => ["http://localhost:9200"]
    index => "menu-service-log-%{+YYYY.MM.dd}"
    template_overwrite => true
  }
}