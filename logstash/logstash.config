input {
  tcp {
      port => 5044
      codec => json_lines
      type => "log"
    }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "menu-service-%{[@metadata][service]}-%{[@metadata][log_level]}-%{+YYYY.MM.dd}"
    template_overwrite => true
  }
}